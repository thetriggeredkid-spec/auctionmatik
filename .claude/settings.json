{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) [ -f \"$f\" ] && /Users/charlescha/auctionmatik/venv/bin/python -m black -q \"$f\" ;; esac"
          }
        ]
      }
    ]
  }
}
